# VK Playbook

Use official VK connector only when token and permissions exist. Otherwise use CSV/manual fallback.

Return:

- `final_url`
- `tracking_link`
- posted URL or tracking slug
- views
- likes
- comments
- shares
- clicks

Do not use another participant's destination or bypass VK permissions.
