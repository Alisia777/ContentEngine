# Аудит контура: что есть, чего не хватает (ночь 24→25.08.2026)

Заказ владельца: «сделать аудит того, чего нам не хватает, и проверить
массовость и очередь». Статусы: ✅ работает · ⚠️ работает с оговоркой · ❌ нет.

## 1. Создание

- ✅ Три маршрута платно доступны; движков в реестре: «Копия» — 7 (Pika
  $0.47/ролик, Kling O3 Std $0.13/с, Kling O3 Pro $0.17/с, Minimax H3 $0.06/с,
  Happy Horse $0.14/с, Seedance 2.5 $0.58/с, Runway Aleph), «Дуэт» — HeyGen
  $0.05/с, «Создание» — 4 fal-движка.
- ⚠️ «Создание» (rebuild) платно ни разу не прожжено — маршруты включены
  23.08, но succeeded-наряда нет. Первый платный прогон = проверка боем.
- ✅ М0 «живая очередь»: следующий ролик ставится, пока предыдущий крутится
  (замок держит только секунды фиксации платежа). Боевую пару запусков
  владелец гоняет после обновления вкладки.
- ❌ М1 «пачка» (до 20 исходников одним подтверждением) — след. сессия,
  карта внедрения готова (см. MASS_GENERATION_QUEUE_2026-08-24.md).
- ❌ Автодобыча исходников: MP4 грузим руками. YouTube-инжест живёт в
  «Исследованиях» и к конвейеру «Копии» не подключён.
- ⚠️ Наследие отладки в статистике отказов: fal 12 failed / 3 succeeded, но
  11 из 12 отказов — эпоха до фиксов потоковой приёмки и опроса (≤21.08);
  после — 3 успеха и 1 честный отказ провайдера. За процентом брака следим
  на первых пачках.

## 2. Проверка

- ✅ Очередь «Ролики на проверке»: плеер, «Принять и разместить» /
  «Отвергнуть» (в корзину с причиной); оператор видит свои, руководство — все.
- ⚠️ В очереди сейчас 14 черновиков + 2 в старом тяжёлом контуре QA — их
  надо разгрести решениями (это работа, не код).
- ❌ Имена карточек — UUID; человеку нужны названия (товар + дата + движок).
- ❌ Авто-предфильтр брака (М3): битый файл, чёрный кадр, тишина.

## 3. Размещение

- ✅ Задача 'placement' + подтверждение финальной ссылки; история — список со
  статусами и датами; ERID обязателен.
- ❌ Реестр аккаунтов ПУСТ (0) — форма «Разместить» упирается в это у всех.
  Первый шаг владельца: «Люди → Аккаунты».
- ❌ Автопостинг: Telegram-бот ждёт токен (BotFather → секрет
  TELEGRAM_BOT_TOKEN), затем YouTube OAuth.
- ❌ Расписание публикаций: placements.scheduled_at существует, никто не пишет.

## 4. Статистика

- ✅ Воронка этапов на «Результатах» (создано → проверка → размещение →
  опубликовано → отвергнуто), кликабельна; публикации с метриками, фильтры,
  CSV; переходы по нашим ссылкам считаются сами.
- ❌ Автосбор просмотров/заказов с площадок — пока ручные снимки; авто
  приедет с подключениями (ТГ-бот отдаёт просмотры поста, YouTube — API).
- ❌ Разбивки по операторам/движкам в воронке нет (появится вместе с пачками).

## 5. Деньги

- ✅ Лимиты $500/день и $500/месяц (policy v3), резерв → квитанция →
  списание/возврат; за всё время: 41 платный наряд, $71.5 списано, $13.3
  возвращено.
- ⚠️ 17 замороженных строк леджера (неизвестный исход у провайдера эпохи
  отладки) — нет формы разбора; деньги не утекают, но висят в отчёте.

## 6. Команда и доступы

- ✅ Вкладка «Команда → Аккаунты» (хранители, выдачи, подключения, счётчики).
- ❌ Выдач аккаунтов нет, потому что нет самих аккаунтов (см. п. 3).

## 7. Интерфейс и доставка

- ✅ Док-конвейер: Создать → Проверить → Опубликовать → Результаты;
  «Процессы» — живой список генераций со статусами и автообновлением.
- ✅ (с этой ночи) Баннер «Рабочее место обновилось» виден поверх окон v4 и
  сам перезапускает свободный интерфейс через 15 с — сутки на устаревшей
  сборке больше не повторятся. Один последний раз нужен ручной Ctrl+F5.
- ⚠️ github.io (Pages) не деплоится — billing lock на GitHub у владельца;
  рабочий портал — локальный 8767.

## Проверка массовости (по фактам прода)

Бюджет: есть ($500/день видно в «Расходах»). Движки: 7 на «Копии», от
$0.06/с. Очередь М0: код в проде, замок больше не душит конвейер — боевой
дубль-запуск за владельцем. Пачка М1: нет (след. сессия). Приёмка: конвейер
проверки готов. Публикация: узкое место — пустой реестр аккаунтов и отсутствие
бота. Вывод: 20–30 роликов/день достижимы уже сейчас руками через М0;
50–100 — после М1 + бота.
