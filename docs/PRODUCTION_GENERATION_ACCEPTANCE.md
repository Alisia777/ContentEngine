# Production acceptance: генерация контента

Актуально на 26 июля 2026 года.

Этот документ отделяет реализованный контракт от фактически пройденной
production-приёмки. Наличие кода, зелёных тестов и доступного баланса не
считается доказательством качества реального результата провайдера.

## Матрица готовности

| Требование | Что уже доказано | Что ещё обязательно |
| --- | --- | --- |
| Фото | Реализован `seedream5_lite`: один точный исходник, квадрат 2048 × 2048, приватное сохранение результата, автоматическая очередь QA. Бесплатная проверка ключа, модели, кредитов и квоты прошла на production. | Один реальный smoke примерно за **$0.04**, визуальная проверка упаковки, этикетки, геометрии и текста. |
| Видео 5 секунд | Реализован `gen4_turbo`: один исходник, одно движение камеры, без речи, фоновая сверка и приватное сохранение MP4. Бесплатная provider-readiness прошла. | Отдельно подтверждённый реальный smoke примерно за **$0.25**, просмотр всего ролика и контент-проверка. |
| Видео 8 секунд со звуком | Реализован `seedance2_fast`: вертикальный формат, короткая реплика, звук, фоновая сверка и приватное сохранение MP4. Бесплатная provider-readiness прошла. | Отдельно подтверждённый реальный smoke примерно за **$2.32**, просмотр и прослушивание всего ролика, контент-проверка. |
| Автоматическое ТЗ | Проверенный исходник фиксирует SKU и название; компилятор сам собирает ограниченный режимом промпт и не переносит неподтверждённые обещания. Ручная правка больше не может уйти как обученное авто-ТЗ. | На каждом реальном smoke сверить, что фактически отправленный промпт соответствует выбранному товару и режиму. |
| Самообучение | Политика использует только точные результаты генерации с независимым решением QA либо одобренные публикации с просмотренным медиа и зрелыми метриками. После шести сопоставимых QA-наблюдений до трёх повторяющихся слабых измерений превращаются в жёстко заданные prompt-guards; произвольный текст findings, комментариев, обещаний и рекомендаций не обучается. Сохранённые структурные hook-паттерны теперь действительно входят в следующее авто-ТЗ. Клиент и Edge связаны gate-версией `2026-07-26.v1`, а сервер повторно проверяет policy hash непосредственно перед платным состоянием. | Повторить авторизованный бесплатный preflight из production-кабинета и затем сверить фактически применённые ракурс, hook и QA-guards на каждом разрешённом платном smoke. |
| Production-доставка | Supabase Edge исполняет версионированный gate. Публичный GitHub Pages проверен после workflow `30195885025`: клиент `v20260726.4`, CSS и index побайтно совпали с проверенными локальными файлами; воспроизводимый Pages-builder собирает полный import-граф и фиксирует SHA-256 в `release-manifest.json`. | Каждый следующий клиентский релиз принимать только после зелёных CI, CodeQL, deploy workflow и проверки фактических `app.js`, импортируемого компилятора и `index.html` на публичном Pages. |
| Минимум ручной работы | Путь автоматизирован от исследования и сценария до авто-ТЗ, provider preflight, фоновой обработки, приватного сохранения и QA. Для готового MP4 браузер сам извлекает 4–5 контрольных кадров, сохраняет durable evidence и восстанавливает незавершённое подтверждение без нового рендера; внешний AI и автоодобрение при этом не запускаются. Единственное ранее использованное назначение площадки подставляется автоматически, а при нескольких вариантах портал ничего не угадывает. | Человек всё равно подтверждает цену, права, рекламную маркировку и финальное качество после полного просмотра со звуком. Эти действия нельзя безопасно автоматизировать догадкой. |
| Регрессия | Последний полный локальный прогон: `1888 passed`; JS syntax и `git diff --check` прошли. | Повторять полный прогон и минимальный smoke после каждой production-доставки без расширения цены или количества результатов. |

## Порядок платной приёмки

1. Зафиксировать точный SKU, исходник с правами, площадку и активную кампанию.
2. Дождаться статуса проверенного обученного авто-ТЗ.
3. Дождаться автоматической бесплатной Runway readiness-проверки; при ошибке использовать ручной retry.
4. Получить отдельное подтверждение точной цены одного SKU.
5. Создать ровно один результат и не повторять POST при долгом ответе.
6. Дождаться фоновой сверки и приватного сохранения файла.
7. Просмотреть результат целиком и пройти обязательный QA.
8. Только после этого считать соответствующий SKU принятым.

До прохождения пунктов 4–7 формулировка «полностью рабочая реальная генерация»
для соответствующего SKU не используется.
